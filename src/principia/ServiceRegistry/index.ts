/**
 * ServiceRegistry Module
 * 
 * Manages the lifecycle of all services in the Principia.js architecture.
 * Services are registered with unique names and started/stopped in order.
 */

export { ServiceRegistry } from './ServiceRegistry';