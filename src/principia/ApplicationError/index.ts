/**
 * ApplicationError Module
 * 
 * Provides a hierarchy of error classes for consistent error handling
 * throughout the Principia.js architecture.
 */

export { ApplicationError, APIError, ValidationError } from './ApplicationError';
export type { ApplicationErrorContext, APIErrorDetails } from './types';