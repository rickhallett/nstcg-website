/**
 * LoggerService Module
 * 
 * Provides centralized logging capabilities for the Principia.js architecture.
 * Implements the singleton pattern to ensure consistent logging throughout the application.
 */

export { LoggerService } from './LoggerService';
export type { LogLevel, LogMetadata } from './types';